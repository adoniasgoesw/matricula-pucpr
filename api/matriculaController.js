// Exemplo de controller para cadastro de aluno
async function cadastrarAluno(req, res) {
  // Implemente a lógica real aqui
  res.status(200).json({ message: 'Aluno cadastrado com sucesso!' });
}

module.exports = { cadastrarAluno }; 